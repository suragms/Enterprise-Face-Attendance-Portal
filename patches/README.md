# Patches

The project directory is not currently a git repository, so a unified `.patch` file was not auto-generated.

To create one after initializing git:

```bash
git diff > patches/hexaattender-audit-fixes.patch
```

See [AUDIT_REPORT.md](../AUDIT_REPORT.md) for the full list of modified files and refactoring suggestions.
